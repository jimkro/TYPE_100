### 🧑‍💻 User :
> 在這裡貼上**問題或指令**...
我希望在每提升5等時，刷新Boss給玩家打。每增加5等級所產生的Boss，都會多一條血條（也就是說，Boss頭上的第一個單字背打完以後，多一條血就會再多刷新一的單字）
---

### 🤖 Gemini:
> 在這裡貼上**Gemini**剛才的回覆內容...
這是一個很棒的進階機制！這會讓遊戲更有階段性的目標感。

我們需要修改 js/logic.js (負責生成 Boss、處理 Boss 換血/復活邏輯) 和 js/renderer.js (繪製 Boss 血條/命數)。

修改重點：

生成邏輯：在 addXP 函式中判定，若等級是 5 的倍數，生成一隻 Boss。

Boss 屬性：

isBoss: true

lives: 命數 = level / 5 (5等1條，10等2條...)。

體型變大、顏色為金色。

必定是射擊怪 (Shooter)。

受傷邏輯：

當 Boss 的單字被消除（無論是被槍打掉、還是被火/雷燒完）時，檢查 lives。

如果還有命：不死亡，扣除一條命，立刻刷新一個新單字，播放音效，並彈出 "NEXT PHASE" 文字。

如果沒命了：才真正死亡。

以下是完整程式碼：
📄 js/logic.js